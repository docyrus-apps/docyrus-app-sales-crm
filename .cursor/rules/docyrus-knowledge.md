# Knowledge Graph Workflow

- Use `docyrus knowledge search` to find relevant documentation before reading or editing code.
- Use `docyrus knowledge section` to read the full section once you have a relevant section id.
- Use `docyrus knowledge expand` whenever the prompt includes `[[refs]]`.
- Keep `docyrus/knowledge/` updated and finish with `docyrus knowledge check`.
